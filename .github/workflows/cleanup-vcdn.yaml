name: VCDN Folder Check (SSH)

on:
  workflow_dispatch:
    inputs:
      folder_name:
        description: "Leave empty to list VCDN folders"
        required: false

jobs:
  vcdn-check:
    runs-on: ubuntu-latest
    timeout-minutes: 120

    steps:
      - name: Setup SSH key
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.VM_SSH_KEY }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          ssh-keyscan -H x.x.x.x >> ~/.ssh/known_hosts

      - name: List folders or count files
        env:
          VM_IP: x.x.x.x
          VM_USER: root
          FOLDER: ${{ github.event.inputs.folder_name }}
        run: |
          SSH="ssh $VM_USER@$VM_IP"

          if [ -z "$FOLDER" ]; then
            echo "📂 Available VCDN folders:"
            $SSH "ls -d /otss/data/vcdn/*/ | xargs -n 1 basename"
            echo ""
            echo "➡ Re-run workflow and provide folder_name"
            exit 0
          fi

          echo "🔍 Counting files in /otss/data/vcdn/$FOLDER"

          $SSH "
            if [ ! -d /otss/data/vcdn/$FOLDER ]; then
              echo '❌ Folder does not exist'
              exit 1
            fi

            echo '📊 Total files:'
            find /otss/data/vcdn/$FOLDER -type f | wc -l
          "
